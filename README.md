# Zonky marketplace checker

Simple application based on Spring Boot runs check every 5 minutes and prints out new loans.
Initial check is triggered for the previous day (-24h).

## Usage

To run application, use:

`mvn spring-boot:run`